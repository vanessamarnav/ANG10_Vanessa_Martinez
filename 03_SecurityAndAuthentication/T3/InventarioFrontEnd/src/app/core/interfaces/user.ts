export interface User {
    email: string;
    password: string;
    firstName: string;
    lastName: string;
    phoneNumber: number;
    status: boolean;
}
export interface signIn {
    email: string;
    password: string;
}

